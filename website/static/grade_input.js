

/*
function change_coursetype(coursetype) {
    course = document.getElementById("coursetype");
    course.innerHTML = coursetype;
    if (coursetype == "GCSE") {
        
    }
    //If a name is chosen, the name of the coursetype's ID will change and the thing is chosen
}
function change_grade(gradelevel) {

}

var select = document.getElementById("selectDay");
var response1 = ("Response if a Day between Monday through Friday is Selected");
var response2 = ("Response if Saturday or Sunday are selected");

var days = ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"];

for (var i = 0; i < days.length; i++) {
  var opt = days[i];
  var el = document.createElement("option");
  el.textContent = opt;
  el.value = opt;
  select.appendChild(el);
}

function getResponse(eleme) {
  var e = document.getElementById("selectDay");
  var strUser = e.options[e.selectedIndex].value;
  if (strUser == 'Saturday' || strUser == 'Sunday') {
    console.log(response2)
  } else {
    console.log(response1);
  }
}*/